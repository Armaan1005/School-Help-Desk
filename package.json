{
  "name": "charbot-v2-local-server",
  "version": "1.0.0",
  "description": "Local static server and proxy for the CharBot V2 site to safely call AI providers.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "vercel-build": "echo 'no build step required'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-fetch": "^3.3.1"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
